# Infrastructure

Docker Compose, CI/CD, and deployment assets will be added in Phase 12 onward.

The local production-shaped stack is in `docker-compose.yml`; cloud deployment guidance is in `docs/deployment.md`.
